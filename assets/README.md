# Local reference assets

This folder holds large, user-provided inputs that are useful during research
but are not versioned with the project:

- `guides/` contains guide scans and OCR exports.
- `videos/` contains gameplay and speedrun reference videos.

Generated frame samples belong in `runs/`, not here. ROMs remain excluded from
Git and are currently kept at the project root for compatibility with existing
run commands.
